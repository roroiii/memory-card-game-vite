export interface IIsCoveredCard {
  openCards: number[];
  matchedCards: number[];
  index: number;
}

export interface IIsStartTheGame {
  openCards: number[];
  matchedCards: number[];
  timeElapsed: number;
}
